# Telegram Бот "Говори по смыслу"

Telegram-бот для курса "Говори по смыслу" - обучающий бот, который помогает пользователям научиться передавать смысл на английском языке, даже если нужное слово вылетело из головы.

## Ссылка на бота

🤖 [@Dimarepitbot](https://t.me/Dimarepitbot)

## Возможности бота

1. **Приветствие и контакт** - Получение контакта пользователя через /start
2. **PDF-гайд** - Отправка авторского гайда по методике
3. **Мини-урок** - Интерактивный пробный урок с 4 заданиями:
   - Задание 1: Тест на понимание контекста (kitchen)
   - Задание 2: Множественный выбор (airport)
   - Задание 3: Аудио-задание (café)
   - Задание 4: Мотивационное сообщение
4. **Призыв к действию** - Переход на платный мини-курс

## Установка и запуск

### Требования

- Python 3.8+
- pip

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/konigunited/dimabot.git
cd dimabot
```

### Шаг 2: Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите:
- `BOT_TOKEN` - токен вашего бота от @BotFather
- `COURSE_URL` - ссылка на страницу курса

### Шаг 5: Добавление медиа-файлов

Поместите в директорию `assets/`:
- `guide.pdf` - PDF-гайд для пользователей
- `audio_task3.mp3` - Аудио для задания 3

**Примечание:** Если файлы отсутствуют, бот будет работать с текстовыми заглушками.

### Шаг 6: Запуск бота

```bash
python bot.py
```

Бот запустится и начнет обрабатывать сообщения.

## Структура проекта

```
dimabot/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация и тексты
├── keyboards/             # Клавиатуры для бота
│   └── inline.py          # Inline-клавиатуры
├── assets/                # Медиа-файлы
│   ├── README.md          # Инструкция по добавлению файлов
│   ├── guide.pdf          # PDF-гайд (добавить вручную)
│   └── audio_task3.mp3    # Аудио для задания 3 (добавить вручную)
├── requirements.txt       # Зависимости Python
├── .env                   # Переменные окружения (не в git)
├── .env.example           # Пример файла .env
├── .gitignore             # Исключения для git
└── README.md              # Этот файл
```

## Технологии

- **aiogram 3.x** - Асинхронный фреймворк для Telegram Bot API
- **python-dotenv** - Управление переменными окружения

## Сценарий работы бота

1. Пользователь нажимает `/start`
2. Бот приветствует и предлагает получить гайд
3. При нажатии "Получить гайд" - отправляется PDF
4. Бот предлагает пройти мини-урок
5. Пользователь проходит 4 интерактивных задания
6. В конце - призыв перейти на полный курс

## Развертывание в продакшене

Для запуска бота на сервере рекомендуется использовать:

- **systemd** - для автозапуска
- **supervisor** - для мониторинга процесса
- **Docker** - для контейнеризации

### Пример с systemd

Создайте файл `/etc/systemd/system/dimabot.service`:

```ini
[Unit]
Description=Dimabot Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/dimabot
Environment="PATH=/path/to/dimabot/venv/bin"
ExecStart=/path/to/dimabot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl daemon-reload
sudo systemctl enable dimabot
sudo systemctl start dimabot
```

## Разработка

### Изменение текстов

Все тексты бота находятся в `config.py`. Измените их там, чтобы обновить содержимое сообщений.

### Добавление новых заданий

1. Добавьте новый текст в `config.py`
2. Создайте новую клавиатуру в `keyboards/inline.py`
3. Добавьте обработчик в `bot.py`

## Токен бота

**Важно:** Токен бота уже настроен:
```
7117117994:AAFkEq46M7hUz2a9Q8IJ2xdnc4yldCpPqBo
```

⚠️ **Не делитесь токеном публично!** Файл `.env` уже добавлен в `.gitignore`.

## Поддержка

Если возникли вопросы или проблемы:
1. Проверьте, что токен бота правильно указан в `.env`
2. Убедитесь, что все зависимости установлены
3. Проверьте логи бота при запуске

## Лицензия

Этот проект создан для курса "Говори по смыслу".
