# Проверка учетных данных ЮKassa

## Проблема

API ЮKassa возвращает ошибку:
```
"description": "Error in shopId or secret key. Check their validity"
"code": "invalid_credentials"
```

Это означает, что либо **Shop ID**, либо **Secret Key** неверные.

## Текущие учетные данные

В `.env` файле:
```
YUKASSA_SHOP_ID=390517924621
YUKASSA_SECRET_KEY=live_XdalYS1_Of-6agxvffAx7kuNPm9oNTeVr4PK8bczx8w
```

## Как получить правильные ключи

### Шаг 1: Войдите в личный кабинет ЮKassa
https://yookassa.ru/my

### Шаг 2: Перейдите в раздел "Интеграция"
Обычно находится в меню слева или в настройках магазина

### Шаг 3: Найдите секцию с API ключами

Там должны быть:

**Shop ID (Идентификатор магазина):**
- Это числовой идентификатор
- Пример: `123456`
- Находится обычно в верхней части страницы интеграции

**Secret Key (Секретный ключ):**
- Длинная строка, начинающаяся с `live_` или `test_`
- Пример: `live_AbCdEfGhIjKlMnOpQrStUvWxYz1234567890`
- Можно перевыпустить (кнопка "Создать новый ключ" или "Перевыпустить")

### Шаг 4: Скопируйте правильные значения

⚠️ **Важно:**
- Копируйте **полностью весь** Secret Key
- Не добавляйте лишних пробелов
- Shop ID должен быть только цифры

### Шаг 5: Обновите `.env` файл

```env
BOT_TOKEN=8256682945:AAHIV558GPcLXBYCLR9B5E75Rxcxim1_3jU
YUKASSA_SHOP_ID=ваш_правильный_shop_id
YUKASSA_SECRET_KEY=ваш_правильный_secret_key
WEBHOOK_URL=https://your-domain.com/webhook
```

### Шаг 6: Перезапустите бота

```bash
# Остановите текущий бот (Ctrl+C в терминале где он запущен)
# Затем запустите снова:
npm run dev
```

## Проверка после обновления

После обновления `.env` выполните тест:

```bash
npx tsx test-payment.ts
```

Если ключи правильные, вы увидите:
```
✅ Платеж успешно создан!
🔗 URL для оплаты: https://yoomoney.ru/checkout/...
```

## Возможные ошибки

### 1. Ключ не активирован
Если магазин новый, возможно нужно:
- Заполнить данные компании/ИП
- Подтвердить email
- Дождаться активации

### 2. Ключ перевыпущен
Если вы ранее создавали ключ и перевыпустили новый, старый больше не работает.

### 3. Неправильно скопирован
Убедитесь что:
- Нет пробелов в начале/конце
- Скопирован весь ключ
- Нет переносов строк

## Альтернатива: Использование мок-платежей

Пока настраиваете ключи, можете временно использовать мок-оплату.

В `src/config.ts` добавьте:
```typescript
export const config = {
  botToken: process.env.BOT_TOKEN || '',
  yukassa: {
    shopId: process.env.YUKASSA_SHOP_ID || '',
    secretKey: process.env.YUKASSA_SECRET_KEY || '',
  },
  webhookUrl: process.env.WEBHOOK_URL || '',
  useMockPayments: true, // ← Временно включить мок-оплату
};
```

И измените `handleBuyPrompt` в `src/handlers/callbacks.ts` для проверки этого флага.
